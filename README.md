# Chronicles #89 — Summer Special

Internal newsletter for CollaborateGlobal. Published as a single self-contained HTML file
(all photos embedded), served from GitHub Pages.

The issue lives at `summer-c9ba2cf9/index.html`, i.e.

    https://<account>.github.io/Chronicles-89/summer-c9ba2cf9/

**Unlisted, not private.** The page carries `noindex/nofollow` and `robots.txt` blocks all
crawlers, and the folder name is unguessable — but anyone with the URL can open it. Do not
put anything confidential in here. See `SETUP.md`.
